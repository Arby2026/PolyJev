# Проверка V3.0 — 2026-09-21

Ветка: `feat/jev-v3.0-enriched-state`, база `7ab4a3d`.

## Выполненные проверки

- `python -m pytest -q`: **120 passed**.
- Полный цикл `forecast_experiment.py --asset BTC --max-jev-cost 0.05`:
  код завершения **0**, рынок `btc-updown-15m-1789984800`,
  окно `1789984800–1789985700` (15:00–15:15, Asia/Yekaterinburg).
- `forecast_report.py "data/forecast_btc_*.jsonl"`: код завершения **0**;
  обработаны старые JSONL и новый V3.
- `.env` загружен через фолбек и исключён из Git. Проверка индекса,
  live JSONL и stdout не обнаружила значения ключа.
- Резолв, границы окон, collector и `run.py` не изменены.
  В `live_trader.py` изменены только загрузка `.env` и сообщение об отсутствии ключа.

## Результат полного рынка

| Метрика | Значение |
| --- | ---: |
| Прогнозы, один Q1 `p_final_up` | 829 |
| Скипы `mid_below_0.10` | 23 / 852, **2.70%** |
| Оценка расходов Jev | **$0.028604016** из $0.05 |
| Средняя задержка Jev | 539 мс |
| Pearson Q1 / distance_from_start_bps | **0.6775**, PASS при пороге 0.75 |
| Pearson Q1 / time_left_sec | **0.8014**, FAIL при пороге 0.75 |
| Средний effective UP/DOWN, прогнозы | 404.51 / 295.49 bps |
| Средний effective UP/DOWN, включая скипы | 411.46 / 288.54 bps |
| Коэффициент fee schedule | 700 bps |
| Принятые BUY события нужного рынка | 2521 |
| Валидные labels 15/30/60/120 секунд | 824 / 811 / 782 / 729 |

Каждая запись прогноза/скипа проверена на новые поля комиссий и соответствие
`fee_effective_<side>_bps = fee_schedule_bps * (1 - mid_<side>)`.
При mid 0.50 это 350 bps; при крайних ценах effective закономерно выходит
за диапазон 300–400 bps. Старого неоднозначного `fees_bps` в новых записях нет.
В state до Jev присутствуют Time left, Flow, Wall, волатильность и комиссии.
Скипы не содержат Jev-полей. Для всех 829 ответов задан только Q1.

Был один временно неготовый CLOB snapshot; обработка продолжилась.
Первый запуск в сетевой песочнице получил HTTP 403 от OpenRouter и был
помечен `session_abort` без успешных прогнозов. Приведённые выше метрики
относятся только к полному повторному запуску вне песочницы.

Порог прогресса по distance достигнут. Изначальная исследовательская цель
|r| < 0.6 ещё не достигнута; зависимость от времени остаётся высокой.
Нужен ещё один рынок для статистики. Один рынок не доказывает рост win rate.

Резолв в момент завершения цикла ещё ожидался; отчёт не подменяет его
направлением текущей цены. До официального резолва распределение flow
для правильных/неправильных прогнозов помечается как недоступное.

Локальные результаты (исключены из Git):
`data/forecast_btc_1789984800.jsonl` и
`data/forecast_btc_1789984800_report.txt`.
